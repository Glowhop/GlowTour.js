---
"@glowhop/styles-tour": patch
---

The default theme lays out the popover as a flex column instead of a three-row grid, so the footer sits right under the content when a step has no title, and a popover with children of your own stacks them in order.
